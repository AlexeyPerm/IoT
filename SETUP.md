# ⚙️ Подробная инструкция по установке и настройке

<div align="center">

**Полное руководство по установке и настройке IoT системы управления температурой печи**

</div>

---

## 📋 Содержание

- [Предварительные требования](#-предварительные-требования)
- [Установка Python Backend](#-установка-python-backend)
- [Настройка Email-уведомлений](#-настройка-email-уведомлений)
- [Запуск в режиме разработки](#-запуск-в-режиме-разработки)
- [Запуск в режиме продакшена](#-запуск-в-режиме-продакшена)
- [Установка Android приложения](#-установка-android-приложения)
- [Настройка подключения Android к серверу](#-настройка-подключения-android-к-серверу)
- [Устранение неполадок](#-устранение-неполадок)

---

## 🔧 Предварительные требования

### Для Python Backend

- **Python 3.7 или выше**
  ```bash
  python --version  # Проверьте версию
  ```
- **pip** (обычно устанавливается вместе с Python)
  ```bash
  pip --version  # Проверьте версию
  ```

### Для Android приложения

- **Android Studio** (последняя версия)
  - Скачайте с [официального сайта](https://developer.android.com/studio)
  - Минимальная версия: Arctic Fox или новее

- **JDK 11 или выше**
  - Обычно устанавливается вместе с Android Studio
  - Проверка: `File → Project Structure → SDK Location → JDK Location`

- **Android SDK** (API Level 24+)
  - Настройте через `File → Settings → Appearance & Behavior → System Settings → Android SDK`

### Для режима продакшена (Raspberry Pi)

- **Raspberry Pi** (любая модель с GPIO)
- **Raspbian/Ubuntu** установленный на Raspberry Pi
- **MCP3008** (АЦП) для чтения температуры
- **Термосопротивление ТСП 100** или аналогичное
- **Реле** для управления нагревом
- Схема подключения оборудования к GPIO

---

## 🐍 Установка Python Backend

### Шаг 1: Клонирование или загрузка проекта

```bash
# Если проект в Git репозитории
git clone <repository-url>
cd IoT

# Или распакуйте архив с проектом
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

**Для режима разработки (по умолчанию):**
```bash
pip install bottle==0.12.25
```

**Для режима продакшена (Raspberry Pi):**
```bash
# Сначала раскомментируйте gpiozero в requirements.txt, затем:
pip install -r requirements.txt
```

### Шаг 4: Проверка установки

```bash
python IOT.py
```

Должно появиться сообщение о запуске сервера на `http://localhost:8080`.

---

## 📧 Настройка Email-уведомлений

### Шаг 1: Копирование примера конфигурации

```bash
# Windows:
copy cred.json.example cred.json

# Linux/macOS:
cp cred.json.example cred.json
```

### Шаг 2: Получение пароля приложения Gmail

1. Перейдите в [Настройки аккаунта Google](https://myaccount.google.com/)
2. Перейдите в **Безопасность** → **Двухэтапная аутентификация** (включите, если не включена)
3. Перейдите в **Пароли приложений**
4. Создайте новый пароль приложения для "Почта" и "Другое (IoT)"
5. Скопируйте 16-значный пароль (он будет использован вместо обычного пароля)

### Шаг 3: Редактирование cred.json

Откройте файл `cred.json` и заполните данные:

```json
{
  "SRC_EMAIL_ADDRESS": "your-email@gmail.com",
  "EMAIL_PASSWORD": "xxxx xxxx xxxx xxxx",
  "DST_EMAIL_ADDRESS": "recipient-email@gmail.com"
}
```

**Важно:**
- Используйте пароль приложения, а не обычный пароль аккаунта
- Для Gmail используйте 16-значный пароль приложения (с пробелами или без)
- Убедитесь, что двухэтапная аутентификация включена

### Шаг 4: Проверка конфигурации

Запустите сервер и попробуйте установить уставку температуры. Если все настроено правильно, при изменении состояния реле должны приходить email-уведомления.

**Примечание:** Если файл `cred.json` отсутствует или настроен неправильно, сервер будет работать, но email-уведомления отправляться не будут. В консоли появятся соответствующие сообщения.

---

## 🛠️ Запуск в режиме разработки

Режим разработки **включен по умолчанию** и не требует дополнительной настройки.

### Быстрый запуск

```bash
# Убедитесь, что виртуальное окружение активировано (если используется)
python IOT.py
```

### Что происходит в режиме разработки

- ✅ Температура генерируется случайным образом (200-1500°C)
- ✅ Управление реле логируется в консоль (без реального переключения)
- ✅ Все функции работают без дополнительного оборудования
- ✅ Работает на любом компьютере (Windows, Linux, macOS)

### Использование API

После запуска сервера доступны следующие эндпоинты:

- **GET `/`** — Веб-интерфейс для управления
  - Откройте в браузере: `http://localhost:8080`

- **GET `/current_temperature`** — Получить текущую температуру (JSON)
  ```bash
  curl http://localhost:8080/current_temperature
  # Ответ: {"temperature": 450.75}
  ```

- **POST `/setpoint`** — Установить уставку температуры
  ```bash
  curl -X POST http://localhost:8080/setpoint \
    -d "setpoint=500"
  ```

---

## 🏭 Запуск в режиме продакшена (Raspberry Pi)

### Шаг 1: Подготовка Raspberry Pi

1. Установите Raspbian/Ubuntu на Raspberry Pi
2. Обновите систему:
   ```bash
   sudo apt update
   sudo apt upgrade -y
   ```

3. Установите Python и pip (если не установлены):
   ```bash
   sudo apt install python3 python3-pip -y
   ```

### Шаг 2: Подключение оборудования

Подключите оборудование к Raspberry Pi согласно схеме:

- **MCP3008 (АЦП):**
  - VDD → 3.3V
  - VREF → 3.3V
  - AGND → GND
  - DGND → GND
  - CLK → GPIO 11 (SPI0 SCLK)
  - DOUT → GPIO 9 (SPI0 MISO)
  - DIN → GPIO 10 (SPI0 MOSI)
  - CS/SHDN → GPIO 8 (SPI0 CE0)
  - Канал 0 → Термосопротивление

- **Реле:**
  - GPIO пин 17 → Вход реле
  - GND → Общий минус
  - VCC → 5V (если требуется)

### Шаг 3: Включение SPI интерфейса

```bash
# Запустите конфигурацию Raspberry Pi
sudo raspi-config

# Выберите: Interface Options → SPI → Enable
# Перезагрузите Raspberry Pi
sudo reboot
```

### Шаг 4: Раскомментирование кода для продакшена

Откройте файл `IOT.py` и найдите следующие секции:

1. **Раскомментируйте импорт:**
   ```python
   from gpiozero import MCP3008, OutputDevice
   ```

2. **Раскомментируйте инициализацию GPIO устройств:**
   ```python
   try:
       relay = OutputDevice(RELAY_PIN)
       adc = MCP3008(ADC_CHANNEL)
       HARDWARE_AVAILABLE = True
       logger.info(f"GPIO устройства инициализированы: реле на пине {RELAY_PIN}, АЦП на канале {ADC_CHANNEL}")
   except Exception as e:
       logger.warning(f"GPIO устройства недоступны: {e}")
       relay = None
       adc = None
       HARDWARE_AVAILABLE = False
   ```

3. **Раскомментируйте реальный код чтения температуры в функции `read_temperature()`**

4. **Раскомментируйте реальный код управления реле в функции `control_heating_relay()`**

### Шаг 5: Установка зависимостей для продакшена

```bash
# Раскомментируйте gpiozero в requirements.txt, затем:
pip install -r requirements.txt
```

### Шаг 6: Запуск сервера

```bash
# Для тестирования:
python IOT.py

# Для постоянной работы (с автозапуском):
# Создайте systemd сервис или используйте screen/tmux
```

### Шаг 7: Проверка работы

1. Проверьте логи при запуске — должно быть сообщение о инициализации GPIO
2. Установите уставку через веб-интерфейс
3. Проверьте, что реле переключается при изменении температуры
4. Проверьте, что приходят email-уведомления (если настроены)

---

## 📱 Установка Android приложения

### Шаг 1: Открытие проекта в Android Studio

1. Запустите **Android Studio**
2. Выберите **File → Open**
3. Перейдите в папку `AndroidApp` внутри проекта IoT
4. Нажмите **OK**

### Шаг 2: Синхронизация Gradle

Android Studio автоматически начнет синхронизацию Gradle. Дождитесь завершения процесса.

**Если возникли ошибки:**
- Убедитесь, что используется JDK 11+
- Проверьте подключение к интернету (Gradle скачивает зависимости)
- Очистите проект: **Build → Clean Project**

### Шаг 3: Настройка эмулятора или подключение устройства

**Вариант A: Использование эмулятора**

1. **Tools → Device Manager → Create Device**
2. Выберите устройство (например, Pixel 4)
3. Выберите системный образ (API Level 24+)
4. Нажмите **Finish**

**Вариант B: Использование реального устройства**

1. Включите **Режим разработчика** на Android устройстве:
   - Перейдите в **Настройки → О телефоне**
   - Нажмите 7 раз на **Номер сборки**

2. Включите **Отладку по USB**:
   - Перейдите в **Настройки → Для разработчиков**
   - Включите **Отладка по USB**

3. Подключите устройство к компьютеру через USB

### Шаг 4: Запуск приложения

1. Выберите устройство в выпадающем списке устройств (вверху)
2. Нажмите кнопку **Run** (▶️) или `Shift + F10`
3. Дождитесь установки и запуска приложения

---

## 🔌 Настройка подключения Android к серверу

### Режим разработки (по умолчанию)

По умолчанию Android приложение использует **симуляцию данных** и не требует подключения к серверу.

### Режим продакшена (подключение к реальному серверу)

#### Вариант 1: Эмулятор Android

Если сервер запущен на компьютере с Android Studio:

1. Откройте `MainActivity.kt`
2. Найдите константу `BASE_URL_EMULATOR` (строка 25)
3. Убедитесь, что используется `"http://10.0.2.2:8080/"` (это специальный адрес, указывающий на localhost хоста)

#### Вариант 2: Реальное устройство в той же сети

1. Узнайте IP-адрес компьютера с сервером:
   ```bash
   # Windows:
   ipconfig
   # Linux/macOS:
   ifconfig
   # Или:
   hostname -I
   ```

2. Откройте `MainActivity.kt`
3. Измените константу `BASE_URL_LOCALHOST`:
   ```kotlin
   private const val BASE_URL_LOCALHOST = "http://192.168.1.100:8080/"  // Замените на ваш IP
   ```

4. В функции `fetchRealTemperature()` замените `BASE_URL_EMULATOR` на `BASE_URL_LOCALHOST`

#### Вариант 3: Сервер в интернете

Если сервер доступен через интернет:

1. Откройте `MainActivity.kt`
2. Замените URL на публичный адрес:
   ```kotlin
   private const val BASE_URL_PRODUCTION = "http://your-server.com:8080/"
   ```

**Важно:** Для доступа через интернет необходимо настроить firewall и убедиться, что порт 8080 открыт.

### Активация реального подключения

1. Откройте `MainActivity.kt`
2. Найдите функцию `fetchCurrentTemperature()` (строка ~90)
3. Раскомментируйте вызов `fetchRealTemperature()` и закомментируйте `simulateNetworkRequest()`:

```kotlin
private fun fetchCurrentTemperature() {
    showLoading()

    // РЕЖИМ РАЗРАБОТКИ: используем симуляцию (по умолчанию)
    // simulateNetworkRequest()

    // РЕЖИМ ПРОДАКШЕНА: для работы с реальным сервером
    fetchRealTemperature()
}
```

4. Пересоберите и запустите приложение

---

## 🔧 Устранение неполадок

### Проблемы с Python Backend

#### Ошибка при импорте gpiozero на обычном ПК

**Проблема:** `ModuleNotFoundError: No module named 'gpiozero'`

**Решение:** Это нормально! Библиотека `gpiozero` работает только на Raspberry Pi. В режиме разработки она не требуется. Убедитесь, что строка с импортом закомментирована, и используйте моки.

#### Ошибка при запуске сервера

**Проблема:** `Address already in use` или порт занят

**Решение:**
```bash
# Windows: найдите процесс, использующий порт 8080
netstat -ano | findstr :8080
# Убейте процесс (замените PID на найденный):
taskkill /PID <PID> /F

# Linux/macOS:
lsof -ti:8080 | xargs kill -9
```

Или измените порт в `IOT.py`:
```python
SERVER_PORT = 8081  # Другой порт
```

#### Email-уведомления не отправляются

**Проблема:** Нет уведомлений, хотя cred.json настроен

**Решение:**
1. Проверьте логи в консоли — там будут сообщения об ошибках
2. Убедитесь, что используется пароль приложения, а не обычный пароль
3. Проверьте, что двухэтапная аутентификация включена в Google аккаунте
4. Проверьте интернет-соединение
5. Проверьте формат JSON в `cred.json` (должен быть валидным)

### Проблемы с Android приложением

#### Приложение не может подключиться к серверу

**Проблема:** "Ошибка соединения" при попытке получить температуру

**Решение:**
1. **Для эмулятора:** Используйте `10.0.2.2` вместо `localhost`
2. **Для реального устройства:** Убедитесь, что устройство и сервер в одной сети Wi-Fi
3. Проверьте, что сервер запущен и доступен:
   ```bash
   # На компьютере с сервером откройте в браузере:
   http://localhost:8080/current_temperature
   ```
4. Проверьте firewall — порт 8080 должен быть открыт
5. Убедитесь, что сервер слушает на `0.0.0.0`, а не только на `127.0.0.1`

#### Приложение не компилируется

**Проблема:** Ошибки Gradle, зависимости не загружаются

**Решение:**
1. Проверьте подключение к интернету
2. Очистите проект: **Build → Clean Project**
3. Пересоберите: **Build → Rebuild Project**
4. Инвалидируйте кеш: **File → Invalidate Caches → Invalidate and Restart**
5. Обновите Gradle: **File → Settings → Build, Execution, Deployment → Gradle**

#### График не отображается

**Проблема:** Пустой экран при открытии истории температуры

**Решение:**
1. Убедитесь, что файл `temperature_data.json` создан и содержит данные
2. Проверьте логи Android Studio (Logcat) на наличие ошибок
3. Убедитесь, что данные передаются в `TemperatureGraphActivity` через Intent

### Проблемы с Raspberry Pi

#### GPIO устройства не инициализируются

**Проблема:** Ошибка при инициализации реле или АЦП

**Решение:**
1. Проверьте физическое подключение устройств
2. Убедитесь, что SPI интерфейс включен: `sudo raspi-config`
3. Проверьте права доступа: пользователь должен быть в группе `gpio`
   ```bash
   sudo usermod -a -G gpio $USER
   # Перелогиньтесь
   ```
4. Проверьте правильность номеров пинов в коде
5. Убедитесь, что используется правильный канал АЦП (обычно 0)

#### Некорректные показания температуры

**Проблема:** Температура не соответствует реальной

**Решение:**
1. Проверьте формулу преобразования в функции `read_temperature()`
2. Проверьте схему подключения термосопротивления
3. Проверьте значения резисторов делителя напряжения
4. Проверьте опорное напряжение АЦП (должно быть 3.3V)

---

## 📞 Дополнительная помощь

Если вы столкнулись с проблемой, не описанной в этом руководстве:

1. Проверьте логи сервера (консоль Python)
2. Проверьте логи Android приложения (Logcat в Android Studio)
3. Убедитесь, что используете последнюю версию кода
4. Проверьте документацию используемых библиотек

---

<div align="center">

**Удачной установки! 🚀**

[← Назад к README](README.md)

</div>
